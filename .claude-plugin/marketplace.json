{
  "name": "noticed",
  "display_name": "noticed — Developer Intelligence",
  "description": "Search developer networks, trace connections, and discover paths through GitHub and LinkedIn graphs.",
  "version": "0.1.0",
  "author": "noticed",
  "skills": [
    {
      "path": "../skills/noticed-search"
    }
  ],
  "mcp_servers": [
    {
      "name": "noticed",
      "description": "noticed MCP server — search developer networks and find connection paths",
      "command": "noticed",
      "args": ["mcp"],
      "env": {
        "NOTICED_API_URL": {
          "description": "URL of your noticed instance",
          "required": true
        },
        "NOTICED_API_KEY": {
          "description": "API key for authentication",
          "required": true
        }
      }
    }
  ]
}
